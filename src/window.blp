using Gtk 4.0;

template UltramarineInstallerWindow : ApplicationWindow {
  default-width: 600;
  default-height: 300;

  Label label {
    label: "AMONG US";
  }

  [titlebar]
  HeaderBar header_bar {
    [end]
    MenuButton {
      icon-name: "open-menu-symbolic";
    }
  }
}
